import React from 'react';

import { Container, Title } from './styles';

const Saved: React.FC = () => {
  return (
    <Container>
      <Title>Tab Salvos</Title>
    </Container>
  );
}

export default Saved;